# PROPUESTAS PENDIENTES — 2026-04-17
> Generadas por OpenClaw AI. Revisar y aprobar antes de implementar.

## Run: 2026-04-17_07-44.md

- [ ] [BUG] GLOBAL: Módulos no cargan, overlay persistente | SOLUCIÓN: Refactorizar el router principal en `main.js` para asegurar que solo un módulo principal se renderiza a la vez. Implementar un sistema de estado global (`appState.js`) para gestionar la visibilidad de modales como "CALIBRACIÓN DIARIA" y "SESION DE LECTURA" de forma condicional, no por defecto. Verificar que no haya un `z-index` excesivo o un `position: fixed` global que tape el contenido. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] GAMIFICACIÓN: Completar tarea no suma XP | SOLUCIÓN: Debuggear la función `completeTask(taskId)` en `flowModule.js`. Asegurar que llama a `firebaseService.updateUserXP(XP_PER_TASK)` con un valor positivo y que el valor de `XP_PER_TASK` no sea 0. Verificar que el valor se persista en Firestore y se refleje en el frontend. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] UX: NLP del FAB clasifica mal gastos como tareas | SOLUCIÓN: Mejorar el modelo NLP en `nlpService.js`. Añadir patrones de regex más específicos para detectar montos y palabras clave financieras ("gasté", "costó", "varos", "pagué", "$", "€") y priorizar la clasificación como "Gasto" o "Ingreso" para el módulo Financiero. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [DISEÑO] LANDING-PAGE: Falta de animaciones y dinamismo en Hero y Módulos | SOLUCIÓN: Implementar `IntersectionObserver` en `main.js` para animaciones de scroll-reveal en las secciones de Módulos y Cómo Funciona. Añadir `@keyframes` CSS en `styles.css` para el `glow pulse` del botón CTA principal y partículas animadas de fondo en el Hero. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] PWA: Pantalla en blanco en modo offline | SOLUCIÓN: Actualizar `service-worker.js` para precachear una página `offline.html` con un mensaje amigable y el branding de Life OS, y servirla cuando la red no esté disponible. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [BUG] FLOW: Navegación del calendario no cambia mes | SOLUCIÓN: Debuggear la lógica de `FlowModule.renderCalendar()` en `flowModule.js`. Asegurar que los eventos de clic en los botones de navegación (`<button class="prev-month">`, `<button class="next-month">`) actualizan correctamente el estado del mes y año, y vuelven a renderizar el calendario con los nuevos datos. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [BUG] FLOW: Hábito de prueba no aparece en lista | SOLUCIÓN: Una vez resuelto el bug de overlay, verificar que `FlowModule.renderHabits()` se ejecuta correctamente y que los datos de hábitos se obtienen de Firestore y se renderizan en el DOM. Asegurar que no haya filtros que oculten los hábitos de prueba. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [IDENTIDAD-VISUAL] MÓDULOS: Falta de identidad visual distintiva | SOLUCIÓN: Una vez que los módulos sean visibles, auditar cada uno. Asegurar que el CSS con `data-module` scope en `styles.css` aplica los `accent-color` definidos a los títulos, tabs activas y botones primarios de cada módulo, para que se sientan únicos y no genéricos. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [ARQUITECTURA] LANDING-PAGE: Flujo de registro/login | SOLUCIÓN: La pantalla de login actual debe integrarse como un modal o un panel lateral dentro de la `section#landing-page` en `index.html`, activado por los botones "Empezar gratis" y "Ya tengo cuenta". Esto evita un hard redirect y mantiene al usuario en el contexto de la landing. | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA
- [ ] [ARQUITECTURA] DASHBOARD: Implementar opt-in para Dashboard Dinámico | SOLUCIÓN: Añadir un toggle "Dashboard Inteligente" en `SettingsModule.js` (tab "Ajustes") que guarde la preferencia en Firestore (`userPreferences.intelligentDashboard`). En `DashboardModule.js`, implementar la lógica para que, si está activo, llame a `gemeloService.getDynamicDashboardOrder()` para reordenar los widgets. | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA

---

## Run: 2026-04-17_11-20.md

- [ ] [BUG] MÚLTIPLES MÓDULOS (Desktop): La mayoría de los módulos de escritorio (Finanzas, Flow, Cuerpo, Mente, World, Ajustes, etc.) muestran el componente "GESTION DE LECTURA" en lugar de su contenido real. Esto es un fallo crítico de renderizado. | SOLUCIÓN: En `main.js`, revisar la lógica de enrutamiento y la visibilidad de los componentes. Asegurar que cada ruta de módulo carga su componente raíz correspondiente y que `GestionDeLectura` (si es un overlay global) se oculta (`display: none` o `visibility: hidden`) cuando no está activo. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] MÚLTIPLES MÓDULOS (Mobile): La mayoría de los módulos en mobile (Finanzas, Flow, Cuerpo, Mente, World, Ajustes, etc.) muestran el modal "CALIBRACIÓN DIARIA" en lugar de su contenido real. Esto es un fallo crítico de renderizado específico para mobile. | SOLUCIÓN: En `main.js`, inspeccionar la lógica que dispara el modal `CALIBRACIÓN DIARIA`. Es probable que un estado global (`isDailyCalibrationActive`) se esté configurando a `true` incorrectamente al navegar a otros módulos en mobile. Asegurar que el modal solo se muestre en el Dashboard o en el flujo de onboarding. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] GAMIFICACIÓN: XP Tareas: Completar una tarea no suma XP (reporte: "XP: 0 → 0"). Esto rompe el bucle de feedback de gamificación y desmotiva al usuario. | SOLUCIÓN: En `main.js`, localizar la función `completeTask()` o `markHabitComplete()`. Asegurarse de que, tras marcar la tarea/hábito como completado, se llame a `updateUserXP(amount)` con un valor `amount` > 0 y que este valor se persista correctamente en Firestore. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] NLP FAB: Clasificación de Gastos: El FAB clasifica entradas de gastos ("gasté 150 en café", "gasoline 500", "50 varos", "pagué el estacionamiento") como "Tarea" en lugar de "Gasto". | SOLUCIÓN: Mejorar la lógica de `parseFABInput(text)` en `main.js`. Implementar detección de palabras clave monetarias (`$`, "varos", "gasté", "pagué", "costó") y patrones numéricos para inferir el intent "Gasto" y extraer el monto. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] NLP FAB: Clasificación de Calendario: El FAB clasifica "llamar al médico esta semana" como "📅 "llamar al médico esta semana" · Hoy → Calendario", asignando "Hoy" incorrectamente. | SOLUCIÓN: Refinar la extracción de entidades de tiempo en `parseFABInput(text)` para el intent `calendar`. La IA de Gemini debe ser capaz de interpretar "esta semana" y asignar un rango de fechas o proponer una fecha dentro de la semana, no por defecto "Hoy". | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [DISEÑO] LANDING PAGE (Mobile Hero): El hero de la landing page en mobile (`responsive-android-landing`, `responsive-ios-landing`) aparece vacío, sin el headline principal ni el CTA, afectando la primera impresión. | SOLUCIÓN: En `styles.css`, revisar las media queries para la sección `#landing-page .hero`. Asegurar que el `h1` principal (`.hero__headline`) y el botón CTA primario (`.hero__cta-primary`) sean visibles y estén correctamente posicionados en viewports de 360-390px, ajustando `font-size` y `padding` si es necesario. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [DISEÑO] LANDING PAGE (Desktop Hero): El texto "Versión 2.0 - Ahora con IA" en el hero de desktop (`00-landing-hero`) es pequeño y se siente como un elemento secundario. | SOLUCIÓN: Reubicar este texto para que tenga más impacto. Podría ser un badge animado junto al logo "LIFE OS" o integrarse de forma más prominente en el subheadline, por ejemplo: "El sistema operativo de tu vida. Impulsado por IA." | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [BUG] CALENDARIO: Navegación: El reporte indica que navegar al mes siguiente no cambia el header ("Abril 2026" → "Abril 2026"), impidiendo la exploración del calendario. | SOLUCIÓN: En `main.js`, localizar la función `navigateCalendarMonth(direction)` dentro del módulo `Flow` (tab `Agenda`). Verificar que la lógica de actualización del estado `currentMonth` y `currentYear` sea correcta y que el DOM se re-renderice con el nuevo mes. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [ARQUITECTURA] IDENTIDAD-VISUAL: Sistema de Temas por Módulo: La identidad visual única por módulo no se está aplicando, lo que hace que la app se sienta genérica. | SOLUCIÓN: Implementar un sistema robusto en `styles.css` y `main.js` que utilice `data-module` attributes para aplicar los estilos específicos de cada módulo (colores de acento, tipografía, fondos) de forma consistente. Esto podría implicar un `themeProvider` en JS que inyecte variables CSS custom properties (`--module-accent-color`) al cambiar de módulo. | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA
- [ ] [ARQUITECTURA] DASHBOARD: Dashboard Dinámico (Opt-in): Implementar la funcionalidad de reordenamiento inteligente del dashboard. | SOLUCIÓN:

---

## Run: 2026-04-17_13-18.md

- [ ] [BUG] GLOBAL: "SESION DE LECTURA" timer reemplaza contenido de módulos | SOLUCIÓN: En `main.js`, revisar la función `renderModule()` y el ciclo de vida de los componentes. Asegurar que el componente del temporizador (`<pomodoro-timer>` o similar) solo se monte y sea visible en el módulo o estado de "Focus Mode" designado, y que no sea un fallback global. Usar `display: none;` o `v-if` (si fuera un framework) condicionalmente en el CSS o JS principal para ocultarlo cuando no esté activo. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] GAMIFICACIÓN: Completar tarea no suma XP | SOLUCIÓN: En `main.js`, dentro de la función `completeTask(taskId)`, verificar que se invoca `awardXP(amount, 'TASK_COMPLETION')` y que la actualización de XP en Firestore se realiza correctamente. Añadir `try-catch` y `console.error` para detectar fallos silenciosos en la escritura de Firestore. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [BUG] UX: FAB NLP clasifica mal entradas (gastos, hábitos, calendario como tareas) | SOLUCIÓN: En `main.js`, refactorizar la función `parseFABInput(text)`. Implementar un sistema de reconocimiento de patrones más sofisticado (regex + keywords) antes de la clasificación por defecto. Priorizar: 1. Gastos (ej: `/\b(gasté|pagué|costó|compré)\s+(\d+)\s*(en|para)?\s*(.+)/i`), 2. Hábitos (ej: "corrí", "medité", "bebí"), 3. Calendario (ej: "esta semana", "mañana"), 4. Tareas genéricas. Asegurar que Gemini AI sea entrenado con más ejemplos de cada categoría. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [DISEÑO] MOBILE: Hero de Landing Page incompleto y touch target pequeño en mobile | SOLUCIÓN: En `styles.css`, añadir media queries para `max-width: 768px`. Asegurar que el `headline`, `subheadline` y el CTA principal "Empezar gratis →" sean visibles y con tamaño de fuente adecuado. Aumentar el `padding` del botón "Iniciar Sesión →" a un mínimo de `12px 20px` para alcanzar un touch target de al menos 44px de altura/ancho. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [ANIMACIÓN] DISEÑO: Landing Page carece de animaciones premium | SOLUCIÓN:
- [ ] [BUG] PWA: Pantalla en blanco en modo offline | SOLUCIÓN:
- [ ] [ANIMACIÓN] UX: Falta de micro-animaciones en modal "Calibración Diaria" | SOLUCIÓN:
- [ ] [RETENCIÓN] LANDING PAGE: Implementar sección "Demo Visual" | SOLUCIÓN: En `index.html` (dentro de `#landing-page`), crear una sección `id="landing-demo-visual"`. Incluir un `div` con un `mockup` de dispositivo (CSS o SVG) y un `img` que rote entre 3-4 screenshots clave de módulos (ej. Dashboard, Flow-Hábitos, Finanzas, Mente-Bitácora). En `main.js`, implementar un carrusel JS simple con `setInterval` y `fade` transitions. El fondo de la sección debe transicionar suavemente al color de acento del módulo activo. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [ARQUITECTURA] DASHBOARD: Definir opt-in y estructura de datos para Dashboard Dinámico | SOLUCIÓN:
- [ ] [IDENTIDAD-VISUAL] GLOBAL: Implementar y verificar identidad visual única por módulo | SOLUCIÓN: Una vez resuelto el bug del temporizador, realizar una auditoría visual completa. En `styles.css`, para cada módulo (`[data-module="nombre-modulo"]`), definir variables CSS para su `accent-color`, `font-family-secondary`, `background-gradient` y `box-shadow-color`. Aplicar estas variables a elementos clave: títulos, tabs activas, botones primarios, bordes de cards. Asegurar que el glassmorphism se adapte al color de acento de cada módulo. | PRIORIDAD: MEDIA | CATEGORÍA: ARQUITECTURA

---

