# PROPUESTAS PENDIENTES — 2026-04-24
> Generadas por OpenClaw AI. Revisar y aprobar antes de implementar.

## Run: 2026-04-24_11-10.md

- [ ] [BUG] GLOBAL: Gemelo Paywall modal bloquea toda la app | SOLUCIÓN: Implementar una lógica de estado global (`showGemeloPaywallModal`) que se inicialice a `false` y solo se active bajo condiciones específicas (e.g., primer acceso al módulo Gemelo, intento de usar una función premium sin suscripción). El modal debe ser dismissible y su estado de visibilidad persistir en el almacenamiento local o en la sesión para evitar reapariciones intrusivas. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [RETENCIÓN] LANDING PAGE: Mensaje de prueba gratuita ausente en hero y CTA | SOLUCIÓN: Añadir un `<p>` o `<span>` con el texto "Sin tarjeta de crédito · 30 días gratis · Cancela cuando quieras" debajo del botón "Empezar gratis →" en la sección hero (`index.html #landing-page .hero`). Estilizarlo con `font-size: 0.9em; color: var(--text-secondary); text-align: center; margin-top: 10px;` en `styles.css`. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [DISEÑO] LANDING PAGE: Header de la landing no alineado con visión de "título de videojuego" | SOLUCIÓN: Aplicar `animation: fadeInScale 0.8s ease-out forwards;` al logo `Life OS` y un `box-shadow` animado (`@keyframes glowPulse`) al botón "Empieza gratis" en `styles.css`. Considerar un fondo de hero con gradiente animado (`@keyframes gradientShift`) o partículas ligeras. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [UX] LANDING PAGE: Falta de scroll indicator animado en el hero | SOLUCIÓN: Añadir un elemento `div` con un ícono de chevron (`<i class="fas fa-chevron-down"></i>`) o SVG animado al final de la sección hero (`index.html #landing-page .hero`). Aplicar una animación de rebote (`@keyframes bounce`) en `styles.css` para indicar interactividad y contenido debajo. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [ANIMACIÓN] LANDING PAGE: Cards de módulos sin animación de entrada ni hover | SOLUCIÓN: Implementar un `IntersectionObserver` en `main.js` para aplicar una clase `.reveal` a las cards de módulos (`index.html #landing-page .modules .card`) al entrar en el viewport. Esta clase debe transicionar `opacity` de 0 a 1 y `transform` de `translateY(20px)` a `translateY(0)` con un `delay` escalonado (100ms por card) en `styles.css`. Añadir `:hover` styles para `transform: translateY(-5px); box-shadow: 0 10px 20px rgba(var(--module-accent-rgb), 0.3);`. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [IDENTIDAD-VISUAL] MÓDULOS: Inconsistencia en la aplicación de la identidad visual por módulo | SOLUCIÓN: Realizar una auditoría visual completa de cada módulo (una vez resuelto el bug del paywall) para asegurar que el `data-module` scope aplica el `accent-color` correcto a títulos, botones primarios y tabs activas, y que la tipografía y el espaciado reflejan la personalidad de cada módulo en `styles.css`. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [ARQUITECTURA] GEMELO: Reevaluar el punto de entrada del Paywall del Gemelo | SOLUCIÓN: El paywall del Gemelo debe activarse *solo* cuando el usuario navega explícitamente al tab "Gemelo" dentro de "Mente & Poder", o intenta acceder a una funcionalidad premium del Gemelo. No debe ser un bloqueo global al inicio de la app. Ajustar la lógica en `main.js` y `services/gemeloService.js`. | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA
- [ ] [ARQUITECTURA] LANDING PAGE: Integración del login/registro como modal en la landing | SOLUCIÓN: Mover el formulario de login/registro a un componente modal (`components/AuthModal.js`). Al hacer clic en "Empezar gratis" o "Ya tengo cuenta" en la landing, abrir este modal superpuesto a la landing en lugar de redirigir. Asegurar que el modal tenga un diseño premium con glassmorphism y transiciones suaves en `styles.css` y `main.js`. | PRIORIDAD: MEDIA | CATEGORÍA: ARQUITECTURA
- [ ] [ARQUITECTURA] DASHBOARD DINÁMICO: Definición de la estructura de datos para la personalización | SOLUCIÓN: Crear una colección `userActivity` en Firestore con documentos `userId`. Cada documento tendrá un sub-documento `moduleUsage` (e.g., `{ "dashboard": { "lastUsed": "timestamp", "count": 120, "dailyAvg": 10 } }`) y `timeOfDayUsage` (e.g., `{ "07-09": ["flow", "dashboard"] }`). Implementar una función `logModuleUsage(userId, moduleName)` en `services/firestoreService.js` para actualizar estos datos en cada interacción. | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA

---

## Run: 2026-04-24_11-37.md

- [ ] [BUG] GLOBAL: Paywall del Gemelo bloquea acceso a todos los módulos | SOLUCIÓN: Revisar `main.js` o el componente `App.js` (o el router guard) que gestiona la visibilidad del `GemeloPotenciadoModal`. Modificar la condición para que el modal solo se muestre cuando el usuario intente acceder explícitamente al tab "Gemelo" dentro del módulo "Mente & Poder" (`currentRoute === '/mente/gemelo'`) y no tenga suscripción activa. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [RETENCIÓN] LANDING PAGE: Implementar HERO con headline potente | SOLUCIÓN: En `index.html`, crear la sección `#hero` con `h1` (ej. "Tu vida tiene un nivel. Súbelo."), `p` (ej. "Hábitos, finanzas, cuerpo y mente. Todo en un solo lugar, gamificado."), y los dos botones CTA. Asegurar animaciones de entrada en cascada (`fade + translate-Y`) y el texto legal "Sin tarjeta de crédito. 30 días gratis. Cancela cuando quieras." visible debajo del CTA principal. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [DISEÑO] LANDING PAGE: CTA "Empieza gratis" sin estilo premium | SOLUCIÓN: Aplicar la clase `btn-primary-accent` (o similar) al botón "Empeza gratis" en el header y en el hero. Asegurar que `styles.css` contenga las reglas para `background-color: var(--color-accent-green); box-shadow: 0 0 15px var(--color-accent-green);` y una `@keyframes` para el `glow-pulse` en el `box-shadow` al `hover` y en `loop` para el hero. | PRIORIDAD: ALTA | CATEGORÍA: MICRO
- [ ] [DISEÑO] LANDING PAGE: Aplicar glassmorphism a cards de módulos | SOLUCIÓN: En `styles.css`, definir una clase `.glass-card` con `backdrop-filter: blur(10px); background-color: rgba(var(--module-color-rgb), 0.15); border: 1px solid rgba(255, 255, 255, 0.1);` y aplicarla a las cards de los módulos en la landing. Asegurar que el `var(--module-color-rgb)` se adapte al color de acento de cada módulo. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [DISEÑO] LANDING PAGE: Título "9 MÓDULOS" cortado en mobile | SOLUCIÓN: En `styles.css` bajo `=== LANDING PAGE ===`, ajustar el `padding` horizontal o `margin-left`/`margin-right` de la sección `.modules-section h2` para asegurar que el texto sea visible en viewports de 360px. Considerar `padding: 0 1.5rem;` o `max-width: calc(100% - 3rem);` para el contenedor. | PRIORIDAD: MEDIA | CATEGORÍA: MICRO
- [ ] [MOBILE] LANDING PAGE: Añadir scroll indicator animado | SOLUCIÓN: En `index.html`, añadir un elemento `div` con un icono de chevron (`<i class="fas fa-chevron-down"></i>`) al final de la sección HERO. En `styles.css`, aplicar una `@keyframes` para una animación de rebote (`translateY`) que indique la posibilidad de scroll. | PRIORIDAD: BAJA | CATEGORÍA: MICRO
- [ ] [ARQUITECTURA] MONETIZACIÓN: Replantear el flujo de monetización del Gemelo | SOLUCIÓN: El Gemelo debe ser una característica *opcional* y *premium* dentro del módulo "Mente & Poder". Permitir a los usuarios acceder y utilizar todas las demás funcionalidades de Life OS (Hábitos, Finanzas, Cuerpo, etc.) de forma gratuita durante el período de prueba. El paywall del Gemelo solo debe aparecer cuando el usuario intente acceder al tab "Gemelo" por primera vez, o cuando intente usar una función específica del Gemelo que requiera suscripción. Considerar un "free trial" del Gemelo por 7 días para usuarios activos. | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA
- [ ] [ARQUITECTURA] LANDING PAGE: Priorizar finalización y lanzamiento | SOLUCIÓN: Completar todas las secciones de la landing page (`HERO`, `MÓDULOS`, `CÓMO FUNCIONA`, `DEMO VISUAL`, `SOCIAL PROOF`, `PRICING`, `FOOTER CTA`) antes de cualquier campaña de marketing. Asegurar que la sección `DEMO VISUAL` utilice screenshots reales e impresionantes de los módulos. Propondría usar screenshots de: **Dashboard** (por su dinamismo), **Flow/Hábitos** (por la gamificación visible), y **World Map** (por la inmersión). | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA
- [ ] [IDENTIDAD-VISUAL] GLOBAL: Implementar y verificar identidad visual única por módulo | SOLUCIÓN: Una vez que los módulos sean accesibles, verificar que cada módulo (Dashboard, Flow, Cuerpo, Finanzas, Mente, World, Stats, Aprende) tenga su `accent color` definido en CSS (`--module-accent-color`) y aplicado a títulos, tabs activas, y botones primarios dentro de su scope `[data-module="nombre-modulo"]`. Asegurar que la tipografía y el espaciado también reflejen la personalidad de cada módulo como se describe en la visión. | PRIORIDAD: ALTA | CATEGORÍA: ARQUITECTURA

---

